formatters:
  simple:
    format: '%(asctime)s %(name)s [%(levelname)s]: %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
    stream: ext://sys.stdout

root:
  handlers: [console]

model:
  # Please change following properties when changing model.name
  #   - sampling.concurrent_requests
  #   - model.params.url (50052 for lb grpc)
  # MegaMolBART Seq2Seq Base_Small_Span_Aug_Half_Draco_nodes_4_gpus_16.nemo
  name: MegaMolBART
  checkpoint_file: Base_Small_Span_Aug_Half_Draco_nodes_4_gpus_16.nemo
  training_data: /data/db/zinc_train_test.sqlite3 #/data/db/cddd_train.sqlit

  # name:  cuchembench.inference.GrpcMegaMolBARTWrapper
  # training_data: /data/db/zinc_train.sqlite3
  # params:
  #   url: nginx:50052

sampling:
  max_seq_len: 512
  sample_size: 10
  db: /data/db/embedding_cache_megamolbart_seq2seq_test.sqlite3
  concurrent_requests: 10

output:
  path: /data/benchmark_output/

metric:
  validity:
    enabled: True
    input_size: 100 #20000
    radius:
      - 0
      - 1

  unique:
    enabled: False
    input_size: ${metric.validity.input_size}
    remove_invalid: False
    radius: ${metric.validity.radius}

  novelty:
    enabled: False
    input_size: ${metric.validity.input_size}
    remove_invalid: False
    radius: ${metric.validity.radius}

  nearest_neighbor_correlation:
    enabled: False
    input_size: -1
    top_k:
      - 50
      - 100
      - 500

  modelability:
    physchem:
      enabled: False
      input_size: -1
      n_splits: 20
      normalize_inputs: True
      return_predictions: True

    bioactivity:
      enabled: False
      input_size: -1 # Number of genes to include, not number of molecules, -1 is all genes
      gene_cnt: -1
      n_splits: 20
      normalize_inputs: True
      return_predictions: True
